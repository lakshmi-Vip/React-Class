const data = [1,2,3,4]

 data.forEach((num) =>{
   console.log(num *2)
})

//forEach(callBackFunction, index, array)

// const colors = ['red','green', 'blue']

// colors.forEach((paint, index, array) => {
//     console.log(`Index: ${index} color:${paint} array: ${array}`)
// })
